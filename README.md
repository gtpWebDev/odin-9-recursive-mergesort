# odin-9-recursive-mergesort

Odin project on recursion to sort an array of numbers

**index.js** contains the mergeSort function

A simple execution from the CLI runs the code

```bash
node index.js
```
